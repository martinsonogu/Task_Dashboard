apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: task-2tier
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 1m
spec:
  ingressClassName: nginx
  rules:
    - host: task-2tier.local
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: task-api
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: task-frontend
                port:
                  name: http
